plugins {
    id "java-library"
    id "org.springframework.boot.conventions"
}

description = 'Quan Common Config Utils'

dependencies {
    api 'org.projectlombok:lombok'
    api 'org.springframework:spring-web'
    api 'org.springframework:spring-context'
}
